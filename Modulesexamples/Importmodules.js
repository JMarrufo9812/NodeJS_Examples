let modules = require ("./Modules");               //importa las funciones desde el directorio inidicado

let pulgadas = 5;
let millas = 4;
let yardas = 3;
let pies = 2;


console.log(pulgadas + "pulgadas son" +                        // imprime los resultados de la operacion 
modules.pulgadasametros(pulgadas) + "metros"                  //utilizamos la variable e imprimimos el resultado de la funcion
);

